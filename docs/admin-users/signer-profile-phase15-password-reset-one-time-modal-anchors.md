# Admin Users / Signer Profile Phase 15 — Password Reset One-Time Modal UI Anchors\n\n## Baseline\n\n- Phase 14 lock: `admin-users-phase14-password-reset-generated-temporary-password-20260623`.\n- Runtime mutation: `false`.\n- Page inspected: `app/admin/users/page.tsx`.\n- Route inspected: `app/api/admin/users/password-reset/route.ts`.\n\n## Findings\n\n- `pageHasPasswordResetRoute`: `True`\n- `pageHasPasswordResetState`: `True`\n- `pageHasPasswordResetSubmit`: `True`\n- `pageHasExistingResetLabels`: `True`\n- `pageHasCopyButton`: `False`\n- `pageHasTemporaryPasswordModal`: `False`\n- `routeReturnsOneTimeTemporaryPassword`: `True`\n- `routeHasCopyRecommendation`: `True`\n- `routeWarningsPresent`: `True`\n\n## Users page password-reset anchors\n\n- L6: `- Edit-user signer/contact/status UI must use PATCH /api/admin/users/signer-profile with preview/apply behavior.`\n- L17: `- row actions: Reset Password, Unlock Login, Clear Failed-Login Lockout`\n- L18: `- reset temporary password must display once in the standard Barsh Matters modal with copy button and one-time warning`\n- L94: `"admin.backups.restorePreview",`\n- L199: `const [passwordResetTargetEmail, setPasswordResetTargetEmail] = useState("");`\n- L200: `const [passwordResetTemporaryPassword, setPasswordResetTemporaryPassword] = useState("");`\n- L201: `const [passwordResetReason, setPasswordResetReason] = useState("");`\n- L202: `const [passwordResetActorEmail, setPasswordResetActorEmail] = useState("dbarshay15@gmail.com");`\n- L203: `const [passwordResetBusy, setPasswordResetBusy] = useState(false);`\n- L204: `const [passwordResetMessage, setPasswordResetMessage] = useState("");`\n- L205: `const [passwordResetResult, setPasswordResetResult] = useState<any>(null);`\n- L224: `const dbUsers = Array.isArray(data?.databasePreview?.users) ? data.databasePreview.users : [];`\n- L225: `const dbRoles = Array.isArray(data?.databasePreview?.roles) ? data.databasePreview.roles : [];`\n- L229: `const previewReady = Boolean(createResult?.ok && createResult?.mode === "preview" && createResult?.wouldCreate?.email === cleanEmail(createEmail));`\n- L230: `const assignPreviewReady = Boolean(`\n- L232: `assignResult?.mode === "preview" &&`\n- L238: `const removePreviewReady = Boolean(`\n- L240: `removeResult?.mode === "preview" &&`\n- L244: `const overridePreviewReady = Boolean(`\n- L246: `overrideResult?.mode === "preview" &&`\n- L252: `const lockoutPreviewReady = Boolean(`\n- L254: `lockoutResult?.mode === "preview" &&`\n- L259: `const passwordResetPreviewReady = Boolean(`\n- L260: `passwordResetResult?.ok &&`\n- L261: `passwordResetResult?.mode === "preview" &&`\n- L262: `passwordResetResult?.wouldReset?.email === cleanEmail(passwordResetTargetEmail)`\n- L265: `async function submitCreateAdminUser(apply: boolean) {`\n- L268: `setCreateMessage(apply ? "Applying guarded create-admin-user request..." : "Previewing guarded create-admin-user request...");`\n- L274: `apply,`\n- L288: `if (apply) {`\n- L296: `setCreateMessage("Preview complete. No AdminUser row was created. Review the result before Apply.");`\n- L306: `async function submitAssignAdminRole(apply: boolean) {`\n- L309: `setAssignMessage(apply ? "Applying guarded assign-role request..." : "Previewing guarded assign-role request...");`\n- L315: `apply,`\n- L327: `if (apply) {`\n- L333: `setAssignMessage("Preview complete. No AdminUserRole row was created. Review the result before Apply.");`\n- L343: `async function submitRemoveAdminRole(apply: boolean) {`\n- L346: `setRemoveMessage(apply ? "Applying guarded remove-role request..." : "Previewing guarded remove-role request...");`\n- L352: `apply,`\n- L364: `if (apply) {`\n- L370: `setRemoveMessage("Preview complete. No AdminUserRole row was deleted. Review the result before Apply.");`\n- L382: `async function submitAdminUserPasswordReset(apply: boolean) {`\n- L384: `setPasswordResetBusy(true);`\n- L385: `setPasswordResetMessage(apply ? "Applying guarded password reset request..." : "Previewing guarded password reset request...");`\n- L386: `const response = await fetch("/api/admin/users/password-reset", {`\n- L391: `apply,`\n- L392: `targetEmail: passwordResetTargetEmail,`\n- L393: `temporaryPassword: passwordResetTemporaryPassword,`\n- L394: `reason: passwordResetReason,`\n- L395: `actorEmail: passwordResetActorEmail,`\n- L399: `setPasswordResetResult({ ...json, httpStatus: response.status });`\n- L401: `setPasswordResetMessage(json?.error || ’Password reset request failed with HTTP ${response.status}.’);`\n- L404: `if (apply) {`\n- L405: `setPasswordResetMessage("Password reset applied. Password was hashed, not displayed, and change-required is set.");`\n- L406: `setPasswordResetTemporaryPassword("");`\n- L407: `setPasswordResetResult(null);`\n- L410: `setPasswordResetMessage("Preview complete. No password hash was changed. Review the result before Apply.");`\n- L413: `setPasswordResetMessage(err?.message || "Password reset request failed.");`\n- L414: `setPasswordResetResult({ ok: false, error: err?.message || "Password reset request failed." });`\n- L416: `setPasswordResetBusy(false);`\n- L420: `async function submitAdminUserLockout(apply: boolean) {`\n- L423: `setLockoutMessage(apply ? "Applying guarded lock/unlock request..." : "Previewing guarded lock/unlock request...");`\n- L429: `apply,`\n- L442: `if (apply) {`\n- L447: `setLockoutMessage("Preview complete. No AdminUser row was changed. Review the result before Apply.");`\n- L457: `async function submitPermissionOverride(apply: boolean) {`\n- L460: `setOverrideMessage(apply ? "Applying guarded permission override request..." : "Previewing guarded permission override request...");`\n- L466: `apply,`\n- L480: `if (apply) {`\n- L485: `setOverrideMessage("Preview complete. No AdminUserPermissionOverride row was saved. Review the result before Apply.");`\n- L501: `<p style={{ margin: "10px 0 0", color: "#475569", lineHeight: 1.5 }}>Guarded administrator user management surface. Active write controls in this phase are Create Admin User, Assign Role, Remove Role, and Permission Override. All use preview/apply mode, require an authenticated administrator session, require an active `\n- L507: `<strong>Mode:</strong> {data?.mode || "loading"} | <strong>Enforcement Enabled:</strong> {enforcementLabel} | <strong>Planning Roles:</strong> {roles.length} | <strong>Planning Users:</strong> {users.length} | <strong>DB Roles:</strong> {data?.databasePreview?.roleCount ?? 0} | <strong>DB Users:</strong> {data?.databas`\n- L512: `<section data-barsh-admin-users-password-reset-card="true" style={cardStyle}>`\n- L514: `<p style={{ margin: "8px 0 0", color: "#475569", lineHeight: 1.5 }}>Phase 12K guarded route. Preview is the default. Apply hashes the temporary password immediately, sets passwordChangeRequired, resets failed login counters, and never returns or displays the password. This does not enable impersonation and does not ena`\n- L518: `<select data-barsh-admin-users-password-reset-target-email="true" value={passwordResetTargetEmail} onChange={(event) => { setPasswordResetTargetEmail(event.target.value); setPasswordResetResult(null); }} style={inputStyle}>`\n- L525: `<input data-barsh-admin-users-password-reset-temporary-password="true" type="password" value={passwordResetTemporaryPassword} onChange={(event) => { setPasswordResetTemporaryPassword(event.target.value); setPasswordResetResult(null); }} style={inputStyle} placeholder="Min 10 + upper/lower/number/symbol" />`\n- L529: `<input data-barsh-admin-users-password-reset-actor-email="true" value={passwordResetActorEmail} onChange={(event) => setPasswordResetActorEmail(event.target.value)} style={inputStyle} placeholder="owner_admin email" />`\n- L533: `<input data-barsh-admin-users-password-reset-reason="true" value={passwordResetReason} onChange={(event) => { setPasswordResetReason(event.target.value); setPasswordResetResult(null); }} style={inputStyle} placeholder="Required reason" />`\n- L537: `<button data-barsh-admin-users-password-reset-preview-button="true" type="button" onClick={() => void submitAdminUserPasswordReset(false)} disabled={passwordResetBusy} style={{ ...secondaryButtonStyle, opacity: passwordResetBusy ? 0.7 : 1 }}>`\n- L538: `Preview Password Reset`\n- L540: `<button data-barsh-admin-users-password-reset-apply-button="true" type="button" onClick={() => void submitAdminUserPasswordReset(true)} disabled={passwordResetBusy || !passwordResetPreviewReady} style={{ ...primaryButtonStyle, opacity: passwordResetBusy || !passwordResetPreviewReady ? 0.55 : 1, cursor: passwordResetBus`\n- L541: `Apply Password Reset`\n- L543: `<span data-barsh-admin-users-password-reset-message="true" style={{ color: passwordResetResult?.ok ? "#166534" : "#991b1b", fontWeight: 900 }}>{passwordResetMessage}</span>`\n- L545: `{passwordResetResult ? <pre data-barsh-admin-users-password-reset-result="true" style={{ marginTop: 12, whiteSpace: "pre-wrap", background: "#f8fafc", border: "1px solid #e5e7eb", borderRadius: 12, padding: 12, maxHeight: 260, overflow: "auto" }}>{JSON.stringify(passwordResetResult, null, 2)}</pre> : null}`\n- L551: `<p style={{ margin: "8px 0 0", color: "#475569", lineHeight: 1.5 }}>Phase 12J guarded route. Preview is the default. Apply changes only AdminUser.status between active and inactive. It blocks locking the last active bootstrapSafe owner_admin user, does not expose passwords, does not impersonate users, and does not enab`\n- L577: `<button data-barsh-admin-users-lockout-preview-button="true" type="button" onClick={() => void submitAdminUserLockout(false)} disabled={lockoutBusy} style={{ ...secondaryButtonStyle, opacity: lockoutBusy ? 0.7 : 1 }}>`\n- L578: `Preview Lock / Unlock`\n- L580: `<button data-barsh-admin-users-lockout-apply-button="true" type="button" onClick={() => void submitAdminUserLockout(true)} disabled={lockoutBusy || !lockoutPreviewReady} style={{ ...primaryButtonStyle, opacity: lockoutBusy || !lockoutPreviewReady ? 0.55 : 1, cursor: lockoutBusy || !lockoutPreviewReady ? "not-allowed" :`\n- L581: `Apply Lock / Unlock`\n- L596: `Read-only audit review for admin-user create, role assignment, role removal, and permission-override activity. Apply actions are audit logged, and permission enforcement remains disabled.`\n- L607: `<p style={{ margin: "8px 0 0", color: "#475569", lineHeight: 1.5 }}>Phase 3 guarded route. Preview is the default. Apply creates only an AdminUser row; it does not assign roles, create permission overrides, enable enforcement, write Clio, send email, generate documents, or change the print queue.</p>`\n- L639: `<button data-barsh-admin-users-create-preview-button="true" type="button" onClick={() => void submitCreateAdminUser(false)} disabled={createBusy} style={{ ...secondaryButtonStyle, opacity: createBusy ? 0.7 : 1 }}>`\n- L640: `{createBusy ? "Working..." : "Preview Create Admin User"}`\n- L642: `<button data-barsh-admin-users-create-apply-button="true" type="button" onClick={() => void submitCreateAdminUser(true)} disabled={createBusy || !previewReady} style={{ ...primaryButtonStyle, opacity: createBusy || !previewReady ? 0.55 : 1, cursor: createBusy || !previewReady ? "not-allowed" : "pointer" }}>`\n- L643: `Apply Create Admin User`\n- L648: `<div style={{ fontWeight: 950, color: createResult?.ok ? "#166534" : createResult ? "#991b1b" : "#475569" }}>{createMessage || "Preview the request before applying. Apply remains disabled until a matching preview succeeds."}</div>`\n- L657: `<p style={{ margin: "8px 0 0", color: "#475569", lineHeight: 1.5 }}>Phase 3 guarded route. Preview is the default. Apply creates only an AdminUserRole join row; it requires an active target user, an active role, an active owner_admin actor, duplicate-assignment prevention, and active bootstrapSafe owner_admin preservat`\n- L684: `<button data-barsh-admin-users-assign-preview-button="true" type="button" onClick={() => void submitAssignAdminRole(false)} disabled={assignBusy} style={{ ...secondaryButtonStyle, opacity: assignBusy ? 0.7 : 1 }}>`\n- L685: `{assignBusy ? "Working..." : "Preview Assign Role"}`\n- L687: `<button data-barsh-admin-users-assign-apply-button="true" type="button" onClick={() => void submitAssignAdminRole(true)} disabled={assignBusy || !assignPreviewReady} style={{ ...primaryButtonStyle, opacity: assignBusy || !assignPreviewReady ? 0.55 : 1, cursor: assignBusy || !assignPreviewReady ? "not-allowed" : "pointe`\n- L688: `Apply Assign Role`\n- L693: `<div style={{ fontWeight: 950, color: assignResult?.ok ? "#166534" : assignResult ? "#991b1b" : "#475569" }}>{assignMessage || "Preview the role assignment before applying. Apply remains disabled until a matching preview succeeds."}</div>`\n- L702: `<p style={{ margin: "8px 0 0", color: "#475569", lineHeight: 1.5 }}>Phase 3 guarded route. Preview is the default. Apply deletes only an AdminUserRole join row; it blocks missing assignments and blocks removing owner_admin from the last active bootstrapSafe owner_admin user. It does not delete users, delete roles, crea`\n- L732: `<button data-barsh-admin-users-remove-preview-button="true" type="button" onClick={() => void submitRemoveAdminRole(false)} disabled={removeBusy} style={{ ...secondaryButtonStyle, opacity: removeBusy ? 0.7 : 1 }}>`\n- L733: `{removeBusy ? "Working..." : "Preview Remove Role"}`\n- L735: `<button data-barsh-admin-users-remove-apply-button="true" type="button" onClick={() => void submitRemoveAdminRole(true)} disabled={removeBusy || !removePreviewReady} style={{ ...primaryButtonStyle, background: "#991b1b", border: "1px solid #991b1b", opacity: removeBusy || !removePreviewReady ? 0.55 : 1, cursor: removeB`\n- L736: `Apply Remove Role`\n- L741: `<div style={{ fontWeight: 950, color: removeResult?.ok ? "#166534" : removeResult ? "#991b1b" : "#475569" }}>{removeMessage || "Preview the role removal before applying. Apply remains disabled until a matching preview succeeds."}</div>`\n- L750: `<p style={{ margin: "8px 0 0", color: "#475569", lineHeight: 1.5 }}>Phase 3 guarded route. Preview is the default. Apply creates or updates only one AdminUserPermissionOverride row; it requires an explicit reason and blocks any block override mapped to administrator lockout safety routes. It does not change roles, enab`\n- L791: `<button data-barsh-admin-users-override-preview-button="true" type="button" onClick={() => void submitPermissionOverride(false)} disabled={overrideBusy} style={{ ...secondaryButtonStyle, opacity: overrideBusy ? 0.7 : 1 }}>`\n- L792: `{overrideBusy ? "Working..." : "Preview Permission Override"}`\n- L794: `<button data-barsh-admin-users-override-apply-button="true" type="button" onClick={() => void submitPermissionOverride(true)} disabled={overrideBusy || !overridePreviewReady} style={{ ...primaryButtonStyle, opacity: overrideBusy || !overridePreviewReady ? 0.55 : 1, cursor: overrideBusy || !overridePreviewReady ? "not-a`\n- L795: `Apply Permission Override`\n- L800: `<div style={{ fontWeight: 950, color: overrideResult?.ok ? "#166534" : overrideResult ? "#991b1b" : "#475569" }}>{overrideMessage || "Preview the permission override before applying. Apply remains disabled until a matching preview succeeds."}</div>`\n- L805: `<section data-barsh-admin-users-db-preview="read-only" style={{ ...cardStyle, overflowX: "auto" }}>`\n- L806: `<h2 style={{ marginTop: 0 }}>Database-Backed Preview</h2>`\n- L807: `<p style={{ color: "#475569" }}>Preview of persisted admin users and roles. These records are not used for enforcement yet.</p>`\n- L821: `<h2 style={{ marginTop: 0 }}>Effective Permissions Preview</h2>`\n- L829: `<section data-barsh-admin-users-write-controls-preview="phase3-mixed" style={{ ...cardStyle, overflowX: "auto" }}>`\n- L831: `<p style={{ color: "#475569", lineHeight: 1.5 }}>Create Admin User, Assign Role, Remove Role, and Permission Override are active in guarded preview/apply mode. Enforcement remains unavailable and separate.</p>`\n- L840: `["Create Admin User", "Requires active admin session, active owner_admin actor, duplicate-email check, active/inactive status validation, preview/apply, and audit logging on apply.", "Active guarded route"],`\n- L841: `["Assign Role", "Requires active admin session, active owner_admin actor, active target user, active role, duplicate-assignment prevention, preview/apply, bootstrap owner preservation, and audit logging on apply.", "Active guarded route"],`\n- L842: `["Remove Role", "Requires active admin session, active owner_admin actor, existing assignment, preview/apply, audit logging on apply, and last active bootstrapSafe owner_admin protection.", "Active guarded route"],`\n- L843: `["Permission Override", "Requires active admin session, active owner_admin actor, known permission key, allow/block action, explicit reason, preview/apply, audit logging on apply, and never-block safety route protection.", "Active guarded route"],`\n\n## Password reset route response anchors\n\n- L7: `generateTemporaryPassword,`\n- L154: `temporaryPasswordWillBeGeneratedOnApply: true,`\n- L155: `passwordReturnedOnlyOnApply: true,`\n- L166: `let temporaryPassword = generateTemporaryPassword();`\n- L167: `let policyErrors = validatePasswordPolicy(temporaryPassword);`\n- L169: `while ((policyErrors.length > 0 || passwordReusesLastThree(temporaryPassword, targetUser.passwordHistoryJson)) && guard < 10) {`\n- L170: `temporaryPassword = generateTemporaryPassword();`\n- L171: `policyErrors = validatePasswordPolicy(temporaryPassword);`\n- L175: `if (policyErrors.length > 0 || passwordReusesLastThree(temporaryPassword, targetUser.passwordHistoryJson)) {`\n- L185: `const passwordHash = hashPasswordForPhase1(temporaryPassword);`\n- L229: `temporaryPasswordStored: false,`\n- L230: `temporaryPasswordReturned: true,`\n- L231: `temporaryPasswordReturnedOnlyOnce: true,`\n- L232: `passwordExposedInAudit: false,`\n- L262: `temporaryPassword,`\n- L263: `temporaryPasswordOneTimeDisplay: true,`\n- L264: `copyButtonRecommended: true,`\n- L265: `warning: "This temporary password is shown once. Copy it now; it is not stored or recoverable.",`\n- L270: `passwordExposedInAudit: false,`\n\n## Phase 16 patch rules\n\n1. Patch only app/admin/users/page.tsx.\n2. When the password-reset apply response includes temporaryPassword and temporaryPasswordOneTimeDisplay, show a standard Barsh Matters modal/popup.\n3. The modal must warn that the temporary password is shown once.\n4. The modal must include a Copy button using navigator.clipboard.writeText.\n5. The modal must not persist the temporary password beyond client-side state for the visible one-time display.\n6. The modal must not change password reset route behavior.\n7. Do not change signer-profile route, lockout route, 2FA route, document-generation behavior, or DOCX templates.\n\n