# Template Builder Phase 6C — Address Block Format

This targeted repair locks full address block tokens to render as:

```text
Street
City, State Zip
```

The resolver now inserts the comma between city and state/ZIP when both locality components exist. The Build Template example output for insurer and adversary full address blocks now shows the same two-line format.
