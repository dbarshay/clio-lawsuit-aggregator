{
  "requiredRoutePlan": "/admin/templates/layout-composition-validation",
  "requiredApiPlan": "/api/admin/templates/layout-composition-validation",
  "requiredPermission": "templates.layoutCompositionValidation.read",
  "requiredSuiteScript": "verify:templates:layout-composition",
  "requiredGuardrails": [
    "read-only",
    "admin access",
    "source type is disclosed",
    "no document-generation path imports the validator stack",
    "no external document-storage service is called"
  ],
  "combinedPlanningItems": [
    "admin route exposure plan",
    "read-only API exposure plan",
    "permission and safety requirements",
    "expected data-flow contract",
    "verifier coverage"
  ]
}
